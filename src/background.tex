\backgroundsetup {
	scale=1,
	angle=0,
	opacity=1,
	color=black,
	contents = {
		\tikz[overlay, remember picture]package
		\draw ($(current page.north west) + (0.445in,-0.5in)$)
		rectangle ($(current page.south east) + (-0.54in,0.42in)$);
	}
}
